############################################################
# AuditPro Backend – .env.example
# Copy this file to .env and adjust values. Never commit your real .env.
############################################################

# Runtime
NODE_ENV=development
PORT=5000

# Database
# Local dev example below. Use a MongoDB Atlas connection string in production.
MONGODB_URI=mongodb://localhost:27017/auditpro

# Authentication / JWT
JWT_SECRET=change_me_in_production_to_a_long_random_string
JWT_EXPIRE=30d

# Frontend Origin (for CORS)
CLIENT_URL=http://localhost:3000

# Python Processor Service URL
# Local dev: run python processor on 5001. In Render set this to the HTTPS URL of that service.
PYTHON_SERVICE_URL=http://localhost:5001

# Logging
LOG_LEVEL=info

# (Optional) Additional future variables could go here (RATE_LIMIT, REDIS_URL, etc.)

# Deployment Notes:
# 1. In Render, set sensitive values (JWT_SECRET, MONGODB_URI) as secrets, not hard-coded.
# 2. Omit PORT on Render; the platform injects PORT automatically (your code should use process.env.PORT || 5000).
# 3. Keep NODE_ENV=production on Render for optimized behavior.
